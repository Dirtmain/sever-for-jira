name: Playwright + RTM Import

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # Checkout project
      - name: Checkout
        uses: actions/checkout@v4

      # Set up Node
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # Install deps + Playwright
      - name: Install dependencies
        run: |
          npm ci
          npx playwright install --with-deps

      # Run Playwright with JUnit reporter
      - name: Run Playwright tests
        run: |
          npx playwright test --reporter=junit,html
        env:
          CI: true

      # Create ZIP archive with XMLs for RTM import
      - name: Archive JUnit results
        run: |
          mkdir -p rtm-archive
          cp -r results.xml rtm-archive/
          cd rtm-archive
          zip -r ../rtm-results.zip .

      # Upload to Deviniti RTM
      - name: Upload test results to RTM
        run: |
          echo "Uploading results to RTM…"

          curl -X POST "$RTM_BASE_URL/api/v2/automation/import-test-results" \
            -H "Authorization: Bearer $RTM_API_TOKEN" \
            -F "projectKey=$RTM_PROJECT_KEY" \
            -F "file=@rtm-results.zip" \
            -F "reportType=JUNIT" \
            -F "name=Playwright Results $(date +'%Y-%m-%d %H:%M')" \
            -F 'treePath=/Automatic/e2e/moduleA'
            -F "jobUrl=$GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"
            -F 'testExecutionFields={ "fields": {"labels" : ["e2etest"]} }' \
            -F 'testCaseFields={"labels": ["automated", "regression"]}
        env:
          RTM_BASE_URL: ${{ secrets.RTM_BASE_URL }}
          RTM_API_TOKEN: ${{ secrets.RTM_API_TOKEN }}
          RTM_PROJECT_KEY: ${{ secrets.RTM_PROJECT_KEY }}
